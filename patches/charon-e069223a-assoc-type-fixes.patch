diff --git a/charon/src/pretty/fmt_with_ctx.rs b/charon/src/pretty/fmt_with_ctx.rs
index 37499e08..1baa6d34 100644
--- a/charon/src/pretty/fmt_with_ctx.rs
+++ b/charon/src/pretty/fmt_with_ctx.rs
@@ -413,10 +413,16 @@ impl<C: AstFormatter> FmtWithCtx<C> for DynPredicate {
             types_outlive,
             trait_type_constraints,
         } = params;
-        assert!(regions.is_empty());
-        assert!(const_generics.is_empty());
-        assert!(regions_outlive.is_empty());
-        assert_eq!(types.len(), 1);
+        // Note: previous versions of this code asserted
+        //   regions.is_empty() && const_generics.is_empty()
+        //     && regions_outlive.is_empty() && types.len() == 1.
+        // These invariants no longer hold: `translate_trait_objects.rs`
+        // deliberately adds one type parameter per associated type when
+        // building poly-mode dyn predicates (see `dyn_self` construction
+        // there). The pretty-printer only consumes `trait_clauses`,
+        // `trait_type_constraints`, and `types_outlive` for its output,
+        // so the asserts were over-defensive. Suppress unused warnings:
+        let _ = (regions, types, const_generics, regions_outlive);
 
         // Format the clauses with their assoc types, e.g. `Iterator<Item = ...>`.
         let mut cstrs_per_clause: IndexVec<TraitClauseId, Vec<String>> =
diff --git a/charon/src/transform/normalize/expand_associated_types.rs b/charon/src/transform/normalize/expand_associated_types.rs
index ffe6489e..ae0be0d7 100644
--- a/charon/src/transform/normalize/expand_associated_types.rs
+++ b/charon/src/transform/normalize/expand_associated_types.rs
@@ -785,7 +785,25 @@ impl<'a> ComputeItemModifications<'a> {
                     .iter()
                     .any(|pat| pat.matches(&self.ctx.translated, &tr.item_meta.name));
             let remove_assoc_types = !is_self_referential && remove_assoc_type;
-            let type_constraints = self.compute_constraint_set(&tr.generics);
+            let mut type_constraints = self.compute_constraint_set(&tr.generics);
+            // Patch (project-local): propagate type constraints from implied
+            // (supertrait) clauses. Mirrors compute_constraint_set's trait_clauses
+            // loop but for tr.implied_clauses, rebased under parent_clause refs.
+            // Without this, constraints like `Mul::Output = Self` on a supertrait
+            // are invisible to child traits referencing grandparent associated types.
+            for clause in &tr.implied_clauses {
+                let trait_id = clause.trait_.skip_binder.id;
+                if let Some(trait_mods) = self
+                    .compute_trait_modifications(trait_id)
+                    .as_processed()
+                {
+                    let base_path = TraitRefPath::parent_clause(clause.clause_id, trait_id);
+                    for (path, ty) in trait_mods.type_constraints.iter_self_paths() {
+                        let path = path.on_tref(&base_path);
+                        type_constraints.insert_path(&path, ty);
+                    }
+                }
+            }
             let mut modifications =
                 ItemModifications::from_constraint_set(type_constraints, remove_assoc_types);
             if modifications.add_type_params {
@@ -862,7 +880,7 @@ impl<'a> ComputeItemModifications<'a> {
             for (clause_id, tref) in timpl.implied_trait_refs.iter_enumerated() {
                 let clause_path = TraitRefPath::parent_clause(clause_id, tref.trait_id());
                 let pred = &tref.trait_decl_ref;
-                if let Some(pred) = pred.skip_binder.clone().move_from_under_binder()
+                if let Some(pred) = Some(pred.clone().erase())
                     // This takes a `&mut self`, so we reborrow it shared below.
                     && let _ = self.compute_trait_modifications(pred.id)
                     && let Some(trait_mods) = self.trait_modifications[pred.id].as_processed()
@@ -924,7 +942,24 @@ impl UpdateItemBody<'_> {
             }
             _ => self.type_replacements.depth(),
         };
-        let ty = self.type_replacements[dbid].find(&path)?;
+        // Try the primary lookup first, then try the alternate base clause
+        // (SelfClause ↔ Local(Bound(0, 0))) as fallback.
+        let ty = self.type_replacements[dbid].find(&path).or_else(|| {
+            let mut alt = path.clone();
+            match &alt.tref.base {
+                BaseClause::SelfClause => {
+                    alt.tref.base =
+                        BaseClause::Local(DeBruijnVar::Bound(DeBruijnId::zero(), TraitClauseId::ZERO));
+                }
+                BaseClause::Local(DeBruijnVar::Bound(dbid, id))
+                    if dbid.index == 0 && id.index() == 0 =>
+                {
+                    alt.tref.base = BaseClause::SelfClause;
+                }
+                _ => return None,
+            }
+            self.type_replacements[dbid].find(&alt)
+        })?;
         let mut ty = ty.clone().move_under_binders(dbid);
         if let TyKind::TraitType(tref, type_id) = ty.kind() {
             let path = TraitRefPath::self_ref(tref.trait_id()).with_assoc_type(*type_id);
