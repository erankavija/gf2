# Workspace-level cargo configuration
#
# Ensures debug-mode tests are not catastrophically slow by optimizing
# dependencies even in dev profile. Our test suite relies on SIMD kernels
# and simulation loops that are 10-100x slower without optimization.

[profile.dev.package."*"]
opt-level = 2

[profile.dev]
opt-level = 1
