[package]
name = "gf2-core"
version = "0.1.0"
edition = "2021"
authors = ["Vesa Kaskivuo <vesa.kaskivuo@iki.fi>"]
description = "High-performance bit string manipulation primitives over GF(2)"
license = "MIT"
keywords = ["bitset", "gf2", "bitvec", "galois-field"]
categories = ["algorithms", "data-structures", "mathematics", "no-std"]
rust-version = "1.80"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(kani)'] }

[dependencies]
# Non-optional: hosts the SSOT scalar `clmul_u64_scalar` that
# `gf2m::barrett::clmul` delegates to. The SIMD feature now only gates
# whether runtime detection wires up the accelerated kernel bundles.
gf2-kernels-simd = { path = "../gf2-kernels-simd" }
rand = { workspace = true, optional = true }
rayon = { workspace = true, optional = true }
image = { version = "0.25", optional = true, default-features = false, features = ["png"] }
serde = { version = "1.0", features = ["derive"], optional = true }
serde_json = { version = "1.0", optional = true }
# Only pulled in when the `test-support` feature is enabled.
proptest = { workspace = true, optional = true }

[dev-dependencies]
criterion = { workspace = true }
gf2-core = { path = ".", features = ["test-support"] }
proptest = { workspace = true }
rand = { workspace = true }

[features]
default = ["rand", "io"]
# Opt-in to SIMD accelerated kernels. The `gf2-kernels-simd` crate is
# always linked (it hosts the scalar `clmul_u64_scalar` SSOT); this
# feature only gates the runtime detection / kernel dispatch paths.
simd = []
# Random generation functions (enabled by default for testing/examples).
rand = ["dep:rand"]
# Parallel algorithms with rayon
parallel = ["dep:rayon"]
# Matrix visualization to PNG images.
visualization = ["image"]
# File I/O support
io = ["dep:serde", "dep:serde_json"]
# Exposes the canonical field axiom test harness (`field::axiom_tests`) to
# integration tests, benchmarks, and downstream crates. Off by default; gets
# auto-enabled under `cargo test` via the self-dev-dependency above.
test-support = ["dep:proptest"]

[[bench]]
name = "bitvec"
harness = false

[[bench]]
name = "matmul"
harness = false

[[bench]]
name = "wide_logical"
harness = false

[[bench]]
name = "scan"
harness = false

[[bench]]
name = "random"
harness = false
required-features = ["rand"]

[[bench]]
name = "sparse"
harness = false
required-features = ["rand"]

[[bench]]
name = "polynomial"
harness = false

[[bench]]
name = "shifts"
harness = false

[[bench]]
name = "rank_select"
harness = false

[[bench]]
name = "polar"
harness = false

[[bench]]
name = "primitive_poly"
harness = false

[[bench]]
name = "generation"
harness = false

[[bench]]
name = "simd_vs_scalar"
harness = false

[[bench]]
name = "matrix_inversion"
harness = false

[[bench]]
name = "m4rm_profile"
harness = false

[[bench]]
name = "m4rm_components"
harness = false

[[bench]]
name = "rref"
harness = false

[[bench]]
name = "rref_profile"
harness = false

[[bench]]
name = "matrix_vector"
harness = false
required-features = ["rand"]

[[bench]]
name = "field_clone"
harness = false

[[bench]]
name = "fp_montgomery"
harness = false

[[bench]]
name = "fp_specialized"
harness = false
required-features = ["simd"]

[[bench]]
name = "gf2m_mul_strategies"
harness = false

[[bench]]
name = "fieldvec_dot_product"
harness = false

[[bench]]
name = "batch_inverse"
harness = false

[[bench]]
name = "soa_batch"
harness = false
required-features = ["simd"]

[[bench]]
name = "field_poly"
harness = false

[[bench]]
name = "gf2m_wide_mul"
harness = false
required-features = ["simd"]

[[bench]]
name = "field_matrix"
harness = false
required-features = ["rand"]

[[bench]]
name = "field_matrix_gemm"
harness = false
required-features = ["rand"]

[[bench]]
name = "field_matrix_fusion"
harness = false
required-features = ["rand"]

[[bench]]
name = "field_sparse_matrix"
harness = false
required-features = ["rand"]
