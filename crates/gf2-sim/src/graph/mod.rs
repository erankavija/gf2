//! Graph API for composing novel stage chains (design doc §9).
//!
//! Owned by `c09d3e95`. This Phase A stub reserves the module home; the
//! `Chain` builder, `connect()` / `build()` with type-checking and cycle
//! detection, and the `register_fallback` machinery land in that task.

// owned by c09d3e95
