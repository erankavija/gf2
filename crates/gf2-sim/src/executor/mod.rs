//! Hybrid CPU/GPU executor (design doc §8 OOM seam, Phase C).
//!
//! Owned by Phase C (`42eac5cc` / `75c22fa8`). This Phase A stub reserves the
//! module home; the scheduler, the OOM-catch / CPU-fallback substitution, and
//! the drain-for-checkpoint contract land in those tasks.

// owned by Phase C (42eac5cc / 75c22fa8)
