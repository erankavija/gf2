//! DVB-T2 BICM preset pipeline (typestate builder + stage adapters).
//!
//! Owned by `81d05bab`. This Phase A stub reserves the module home; the
//! DVB-T2 `Pipeline::builder()` typestate, the `Modcod` / `Channel` selectors,
//! and the `adapters::*` stage constructors (design doc §9) land in that task.

// owned by 81d05bab
