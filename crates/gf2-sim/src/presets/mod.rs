//! Typestate preset builders for standard pipelines (design doc §9).
//!
//! The typestate builder framework lives here; the per-standard preset
//! content lives in submodules. Phase A reserves the module homes.

// owned by 81d05bab (typestate builder framework)

pub mod dvb_t2;
pub mod nr_5g;
