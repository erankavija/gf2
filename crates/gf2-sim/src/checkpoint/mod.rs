//! v2 heartbeat-checkpoint schema (design doc §4).
//!
//! Owned by `5f12e7ff`. This Phase A stub reserves the module home; the v2
//! `Reader` / `Writer`, the `worker_states[]` drain-commit contract, and the
//! `config_hash` (blake3) machinery land in that task.

// owned by 5f12e7ff
