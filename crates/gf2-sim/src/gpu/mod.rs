//! HIP/ROCm host-side GPU dispatch (design doc §5/§6, `feature = "hip"`).
//!
//! Owned by Phase B (`36075e4c`). This Phase A stub reserves the module home;
//! the `HipDispatcher`, `HipStream`, and `HipStreamPool` host types (and the
//! multi-arch gfx dispatch in design doc §6) land in that task, gated behind
//! `feature = "hip"`.

// owned by 36075e4c (Phase B; feature = "hip")
