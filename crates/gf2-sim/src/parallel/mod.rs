//! Per-worker parallel dispatch and ChaCha20 per-worker seek (design doc §3).
//!
//! Owned by `3fcb7025`. This Phase A stub reserves the module home; the
//! worker-dispatch loop, the `worker_offset` seek scheme, and the
//! worker-index-ordered reducer land in that task.

// owned by 3fcb7025
