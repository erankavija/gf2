{
  "id": "0d9cb8e3-1a83-4079-a6f9-bf59e35cba05",
  "title": "CPU-vs-GPU byte-identity regression for DVB-T2 chain",
  "description": "## Goal\n\nRegression test asserting byte-identity of the four deterministic\ncolumns across {CPU-only, CPU-parallel-24-thread, CPU+GPU} executions\nat the same seed for all six in-scope DVB-T2 MODCODs. Lands the\nCLAUDE.md amendments that close Story `5d0a3fad` (Phase D).\n\n## Context\n\nTask under story `5d0a3fad` (DVB-T2 BICM application) within epic\n`gf2-sim` (`f9717e7e`). Phase 0 design doc:\n`dev/active/ec530af9-pipeline-design.md` §11 (determinism contract).\nWave D.3: depends on `bbf6b6ee`. This is the Phase D closing task\nthat lands the CLAUDE.md amendments per Q5 decision.\n\n## Deliverables\n\n1. `crates/gf2-sim/tests/dvb_t2_regression.rs`:\n   - For each of the six DVB-T2 MODCODs (3 rates × 2 modulations):\n     - Run 200 frames at a fixed seed and a fixed Es/N0 near the\n       smoke-knee from `dev/benchmarks/dvb_t2_awgn/smoke/`.\n     - Mode A: `Pipeline::dvb_t2()...parallelism(1).build()` (CPU-only).\n     - Mode B: `Pipeline::dvb_t2()...parallelism(24).build()`\n       (CPU-parallel).\n     - Mode C: `Pipeline::dvb_t2()...with_gpu(true).build()` (CPU+GPU);\n       only compiled under `#[cfg(feature = \"hip\")]`.\n     - Assert byte-identity of `fer / frames / errors / mean_iters`\n       across A, B, (C when present).\n2. The GPU half of the regression is `#[cfg(feature = \"hip\")]`-gated\n   so the default CI matrix passes without HIP. The gfx1030 CI job\n   runs the GPU half. Resolves audit finding M5.\n3. CLAUDE.md amendments (Phase D close):\n   - Update the DVB-T2 BICM AWGN campaign section to reference the\n     new `gf2-sim`-backed binary path (the legacy\n     `SimulationRunner::run_with_decoder` reference remains for\n     historical context).\n   - Mark `dev/benchmarks/dvb_t2_awgn/` legacy with forward-pointer\n     to `dev/benchmarks/gf2-sim/`.\n4. Receipts:\n   - Update `dev/benchmarks/dvb_t2_awgn/PLAN.md` with a closing\n     note pointing forward to the new pipeline and to\n     `dev/benchmarks/gf2-sim/cpu-foundation-receipts.md` for the\n     parallel throughput numbers (per project plan §11).\n\n## Success Criteria\n\n- [hard] Regression test asserts byte-identity of\n  `fer / frames / errors / mean_iters` across {CPU-only,\n  CPU-parallel-24-thread, CPU+GPU on gfx1030} at the same seed\n  for all six DVB-T2 MODCODs.\n- [hard] GPU half of the regression is `#[cfg(feature = \"hip\")]`-\n  gated; default CI passes without HIP.\n- [hard] CLAUDE.md updated with the two items above (legacy\n  forward-pointer + new binary path reference).\n- [hard] `dev/benchmarks/dvb_t2_awgn/PLAN.md` has the closing\n  forward-pointer note.\n- [hard] `cargo-ci`, `code-review`, `tests` gates green.\n\n## Non-goals\n\n- BER inclusion in byte-identity (excluded per status-quo).\n- Production sweep with ≥ 100 errors at bracket points (owned by\n  `e4849f07`).",
  "state": "backlog",
  "priority": "high",
  "assignee": null,
  "dependencies": [
    "bbf6b6ee-8ddd-420a-9e56-0319297aac55"
  ],
  "gates_required": [
    "cargo-ci",
    "code-review",
    "tests"
  ],
  "gates_status": {},
  "context": {},
  "documents": [],
  "labels": [
    "type:task",
    "component:gf2-coding",
    "epic:gf2-sim"
  ],
  "created_at": "2026-06-05T22:04:45.458919153+00:00",
  "updated_at": "2026-06-06T21:33:15.582001007+00:00"
}