{
  "assignee": "agent:worker",
  "claimed_at": "2026-08-09T14:01:59.013438126Z",
  "context": {},
  "created_at": "2026-08-09T11:54:10.417169574Z",
  "dependencies": [
    "f3dc1bb1-526f-42e2-860a-66d0d0e8279b"
  ],
  "description": "The published shape of a permanent zero-fraction dataset: a versioned campaign home, the root manifest schema, the per-shard and per-summary record schemas, the writer-role partition, and the documentation that lets a reader navigate it.\n\n## Background\n\nNo manifest convention exists anywhere in this repository: a search for manifest files under the documentation tree returns nothing. The closest precedent, a per-curve README generated by an existing campaign binary, records invocation, configuration, host and wall-clock but records no git revision and no toolchain version. The repository requires every published number to trace to a committed artefact recording seeds, git revision, hardware and toolchain, so this schema is new work rather than an adaptation.\n\nThe dataset home is `dev/simulation_results/permanent-zero-fraction/<campaign-id>/`. This is a deliberate divergence from the earlier design sketch, which proposed `dev/campaigns/`: both directories are declared permanent documentation areas, but the development index classifies `dev/campaigns` as simulation campaign *definitions* and `dev/simulation_results` as campaign *outputs*, and today `dev/campaigns` holds only campaign configuration files. A published dataset is an output.\n\nTwo structural rules travel with the layout. Each file in the raw set has exactly one writer role, so a field's execution owns its own paths and campaign-scoped paths belong to finalization; concurrent field executions then never write one file. And the tree is partitioned into raw data and derived artefacts, because an integrity file covering documents that quote it could never be closed.\n\nAn earlier review cycle in this problem family recorded the cost of mixing interpretation into generated output: receipts carrying interpretive prose had to be regenerated every time a claim's wording changed, which was its largest single source of rework. The schema therefore admits data and mechanical provenance fields only.\n\n## Success Criteria\n\n- [hard] REQ-01: A dataset lives at `dev/simulation_results/permanent-zero-fraction/<campaign-id>/` and contains a root manifest, per-shard records under `shards/q<q>/n<nn>/`, one summary per field under `summaries/`, a pooled `summary.csv`, and an integrity file.\n- [hard] REQ-02: The root manifest schema carries the campaign root seed; the stream purpose namespace with each purpose's tag; the cell grid with per-cell $N$, shard size and shard identities; the backend selected per cell; whether the determinant companion runs at each cell; the git revision; the compiler and accelerator runtime versions; the CPU and GPU models; and a schema version.\n- [hard] REQ-03: A per-shard record schema carries the stream address, matrix count, permanent zero count, and the histogram of permanent values over the $q$ residue classes.\n- [hard] REQ-04: A record and a summary row express the determinant companion as a sample count with a zero count, or as an explicit not-evaluated state; a cell that ran without the companion never stores a numeric zero count, which would be indistinguishable from a cell that evaluated its matrices and found none singular.\n- [hard] REQ-05: A summary row schema carries, per $(q,n)$, the pooled matrix count, permanent zero count, the determinant fields of REQ-04, and one typed terminal state. A completed state carries point estimates with intervals and acceptance verdicts; a halted state carries only its canonical reason and no final estimates or verdicts.\n- [hard] REQ-06: The schema partitions the tree into raw data — manifest, shard records, field summaries, pooled summary — and derived artefacts, and names which is which.\n- [hard] REQ-07: The schema assigns each file in the raw set exactly one writer role, with field-scoped paths written by that field's execution and campaign-scoped paths written only by finalization.\n- [hard] REQ-08: A schema-conformance check accepts a well-formed dataset and rejects one with a missing required field, an unknown field, or a wrong schema version, exercised in both directions by test.\n- [hard] REQ-09: No schema field admits an interpretive statement about what the numbers mean, asserted by a test over the schema's own field list.\n- [hard] REQ-10: The layout, the manifest schema, the record schemas, the writer-role partition, the raw-versus-derived boundary, and the rule that a campaign id is never overwritten in place are documented at the dataset home.\n\n## Notes\n\nStorage stays $O(\\text{shards})$ because matrices are regenerable from their stream address; a whole cell costs tens of kilobytes.\n",
  "documents": [
    {
      "assets": [
        {
          "asset_type": "local",
          "content_hash": "cb38eff14d2e0e2a773401472cdbedc2684a806c3216047322988459ecf4644d",
          "is_shared": false,
          "mime_type": "text/markdown",
          "original_path": "protocol.md",
          "resolved_path": "dev/simulation_results/permanent-zero-fraction/protocol.md"
        }
      ],
      "commit": "20a3fb30",
      "doc_type": "implementation",
      "format": "markdown",
      "label": "Dataset schema and layout",
      "path": "dev/simulation_results/permanent-zero-fraction/README.md"
    }
  ],
  "done_at": "2026-08-09T19:20:37.938072503Z",
  "first_ready_at": "2026-08-09T11:54:10.417169574Z",
  "gates_required": [
    "cargo-ci",
    "code-review",
    "doc-review"
  ],
  "gates_status": {
    "cargo-ci": {
      "status": "passed",
      "updated_at": "2026-08-09T18:22:36.042617215Z",
      "updated_by": "auto:executor"
    },
    "code-review": {
      "status": "passed",
      "updated_at": "2026-08-09T18:27:43.807716357Z",
      "updated_by": "auto:executor"
    },
    "doc-review": {
      "status": "passed",
      "updated_at": "2026-08-09T18:30:02.025036561Z",
      "updated_by": "auto:executor"
    }
  },
  "id": "5758790d-421b-47d2-a6f0-c40d1c5dc465",
  "labels": [
    "type:task",
    "epic:permanent-statistics",
    "component:gf2-sim",
    "satisfies:REQ-03"
  ],
  "priority": "high",
  "state": "done",
  "title": "Versioned dataset layout and record schemas",
  "updated_at": "2026-08-09T19:20:37.938072503Z"
}