# Hermetic aff3ct source build (clone + cmake build tree). Never committed.
.aff3ct-build/
# Scratch run outputs (per-point raw CSVs, logs). The committed deliverables
# are the curated *-vs-aff3ct.csv files, the PNGs, and README.md only.
scratch/
*.log
# AList H-matrix exports are regenerated by export_alist; keep them out of git
# (large; checksums recorded in README.md instead).
*.alist
