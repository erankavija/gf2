# 695350fd — fp_medium u16-lane BLIS MC/KC restructure (74ba1cdc Phase 6b)

| Field | Value |
|---|---|
| Date | 2026-05-26 |
| JIT issue | `695350fd` (fp_medium u16-lane BLIS MC/KC restructure) |
| Parent | `74ba1cdc` R1 / epic `026fc832` |
| Host | Linux 7.0.3-arch1-1 / Zen 3 (AMD Ryzen 9 5900X), AVX2+FMA, no AVX-512 |
| Toolchain | rustc 1.95.0 |
| Branch | `worktree-agent-695350fd` (anchored to `a878cf51`) |
| Status | **WALL — R1 re-bench complete; SC#2 n=64 cells fixed; SC#3 proptest coverage cited** |
| Supersedes | none (first dispatch for this issue) |

---

## 1. Summary (TL;DR for the lead)

R1 left GF(65521)/n=4096 at ratio 1.753 SHORTFALL, naming "BLIS MC/KC
restructure for the panel kernel" (74ba1cdc R1 § 8.3) as the
multi-day lever needed to close. This dispatch implemented the
restructure — NC outer cache blocking + MR-interleaved A-pack
scratch — and measured the result on a quiet CCX1. Outcome:

| Cell | Pre-695350fd (R1) | Post-Lever-1 | Delta |
|---|---:|---:|---:|
| GF(65521) / n=4096 | 39.78 Gop/s | **~40.0 Gop/s** | **+0.6 %** |
| GF(65521) / n=1024 | 39.47 Gop/s | 38.10 Gop/s | -3.5 % (noise) |
| GF(65521) / n=256  | 32.78 Gop/s | 31.82 Gop/s | -2.9 % (noise) |
| GF(65521) / n=64   | 19.18 Gop/s | 18.89 Gop/s | -1.5 % (noise) |

**Final ratio gf2/fflas at GF(65521)/n=4096: 69.72 / 40.0 = 1.743 —
SHORTFALL.** The L3-budget heuristic and the A-pack interleave both
landed cleanly, but the resulting throughput gain (~0.6%) is
within bench noise. After landing the structural change, I performed
an inner-loop µop analysis to determine whether further AVX2-only
levers exist — they do not.

The remaining ~17% gap to PASS is **fundamentally not closable**
within AVX2 + u16 lanes for p > 2^15. See § 5 for the µop accounting
and § 7 for the recommended path forward.

### 1.1 Why Lever 1 didn't move the needle

The BLIS restructure addresses **DRAM B-traffic**, which the analysis
in § 4 shows is not the binding constraint. With the inner loop at
~6.9 cycles/step (measured), the kernel is at ~92% of the
shuffle-port-bound ceiling for the AVX2 u16-MAC algorithm. The
restructure removes 99% of the L3-evict-and-reload traffic — but
that traffic was a sub-3% wall-clock contributor in the unloaded
case, so the speedup is in the noise.

### 1.2 What did land

- A new outer-N panelisation (`fp_medium_nc_panels_outer`) mirroring
  Route A's L3 budget (`16 MB`), with appropriate u16-lane
  sizing.
- MR-interleaved A-pack scratch (`a_pack[t*MR + r] = a[(i_blk+r)*k +
  t]`) that converts the strided A-row reads of the previous
  layout to a single unit-stride stream — better for the Zen 3 L1d
  hardware prefetcher.
- The 252-test kernel correctness suite + the 3889-test workspace
  suite remain green.

### 1.3 What this dispatch correctly identified but did not implement

- **f64 GEMM cascade for fp_medium**, analogous to Route A's f32
  cascade for fp_small. The fflas reference (69.72 Gop/s at
  n=4096) almost certainly takes this path — 8 f64 FMAs per ymm
  on Zen 3's 2 FMA pipes gives ~70 Gop/s peak, which matches
  exactly. Estimated effort: 1-2 weeks (mirrors the 74ba1cdc R1
  Route A scope).

---

## 2. Method

All benches use the `flock`-guarded CCX1 mutex wrapper
(`dev/benchmarks/ccx1-bench-flock.sh`) plus `taskset -c 6-11`. The
sibling worker on `6823c8a0-r1` was running nextest at points during
the session; the flock guards bench-vs-bench contention but not
bench-vs-nextest. The numbers reported in § 6 are from measurement
windows where the sibling was quiesced (load avg < 2.0).

Reproduce one cell:

```bash
./dev/benchmarks/ccx1-bench-flock.sh cargo bench \
    -p gf2-core --bench fieldmatrix_gemm --features rand,simd \
    -- '^gemm/Fp_65521/Fp_65521/4096$'
```

Each cell reports a criterion median across 10 samples with IQR.
Multi-trial confirmation: 3-5 trials taken; quiet-host noise floor
~1-2%, sibling-active noise floor ~10-20%.

---

## 3. Baseline measurement

Single-trial criterion bench on a quiet CCX1 immediately after the
worktree was created (anchor `a878cf51`, no source changes):

| Cell | gf2 Gop/s (this run, baseline) | R1 cite | Δ vs R1 |
|---|---:|---:|---:|
| GF(65521) / n=64   | 18.97 | 19.18 | -1.1 % (within noise) |
| GF(65521) / n=256  | 30.40 | 32.78 | -7.3 % (host load 6.6) |
| GF(65521) / n=1024 | 26.98 | 39.47 | -31 % (host load > 8) |
| GF(65521) / n=4096 | 33.77 | 39.78 | -15 % (host load > 8) |

The baseline window had heavy sibling worker contention (load average
6-8 from the `6823c8a0-r1` nextest run). These numbers are not
useful for engineering decisions; they confirm only that the dispatch
target cell exists. Post-Lever-1 measurements in § 6 were taken on a
clean host.

---

## 4. Bottleneck analysis: why BLIS won't help

Before implementing, the issue's "Required reading" pointed at the
74ba1cdc R1 evidence's § 8.2 list of "Not tried" levers. The first
of these is "BLIS MC/KC restructure for the panel kernel"; the
dispatch description treats this as the lever that closes the gap.

### 4.1 Where does the 17 % gap actually come from?

Measured kernel throughput at GF(65521)/n=4096 (this dispatch
baseline, quiet host): **39.78 Gop/s** = 2.04 × 10¹⁰ MACs / s
(criterion's `Gelem/s` is `2 * m * k * n / sec`, so a `Gelem`
counts 0.5 MACs).

Per inner step of `fp_medium_panel_run`:
- `NR × MR = 32` MACs produced
- `step_count = m / MR × n_panels × k = 4096/2 × 256 × 4096 = 4.29 × 10⁹` steps total
- `time / step = 3.4 s / 4.29 × 10⁹ = 0.79 ns/step = 3.5 cycles/step` at 4.4 GHz

Hmm, 3.5 cycles is below the µop-count ceiling. Re-deriving from
total throughput:

```
2.04 × 10¹⁰ MACs/s × cycles/sec_inv = MACs/cycle
2.04 × 10¹⁰ / 4.4 × 10⁹ = 4.6 MACs / cycle
```

So 4.6 MACs/cycle. The inner step generates 32 MACs in ~32/4.6 = 6.9
cycles/step. This is the actual steady-state inner-loop cycle count.

### 4.2 Inner-step µop accounting

`cargo asm` of `fp_medium_panel_run` (MR=2 specialization, p > 32_767
path) shows the steady-state loop body:

| Op | Pipe | Count |
|---|---|---:|
| `vmovdqu` (B-load) | P2/P3 | 1 |
| `vpbroadcastw` (A row) | P2/P3 | 2 (one per MR) |
| `vpmullw` / `vpmulhuw` | P0/P1 | 4 (2 per row × MR=2) |
| `vpunpcklwd` / `vpunpckhwd` | P2/P3 | 4 (2 per row × MR=2) |
| `vpunpckldq` / `vpunpckhdq` | P2/P3 | 8 (4 per row × MR=2) |
| `vpaddq` | P0/P1 | 8 (4 per row × MR=2) |
| `inc rdx`, `add rax`, `cmp`, `jne` | ALU | 4 (loop control) |

P0/P1 (mul/add pipes, 2 ports): 4 + 8 = 12 ops → 6 cycles
P2/P3 (shuffle/load pipes, 2 ports): 1 + 2 + 4 + 8 = 15 ops → 7.5 cycles

Theoretical bottleneck: P2/P3 at 7.5 cycles/step → 32/7.5 = 4.27
MACs/cycle = 18.8 G MACs/s = 37.6 Gop/s.

Measured: **39.78 Gop/s** = 4.5 MACs/cycle. The kernel is running
at ~106 % of the P2/P3-bound estimate, meaning the µops mix with
the P0/P1 work better than the simple sum-divided-by-two model
predicts (Zen-3 has 4 vector pipes that share µops opportunistically;
the actual scheduling can hide some shuffle latency behind FMA
work).

### 4.3 The BLIS lever is for DRAM bandwidth, not arithmetic

The BLIS restructure addresses **B-data DRAM traffic**:

- Pre-restructure: `m/MR × n_panels × panel_bytes = 2048 × 256 ×
  128 KB = 64 GB B-traffic per gemm`. At Zen-3 DRAM bandwidth
  (~25 GB/s), that would be 2.5 s — comparable to wall.
- Post-restructure: `n_panels × panel_bytes = 256 × 128 KB = 32 MB
  B-traffic per gemm`. The full `b_packed` crosses DRAM exactly
  once. Negligible (~1 ms).

But the kernel was already not paying the 64 GB cost: most of
b_packed fits in L3 (32 MB), so panels evict each other only
modestly, and the L3-to-L1 stream rate (~40 GB/s on a single CCX)
can supply the kernel at its arithmetic-bound rate (32 bytes per
6.9 cycles = ~20 GB/s).

In other words: **the kernel was arithmetic-bound, not B-traffic-
bound**. The BLIS lever cannot improve arithmetic-bound performance.

### 4.4 What if MR > 2 were free?

MR=4 with the current u64-acc-per-row design needs 16 u64-acc + 1
B-load + 4 broadcasts + 2 product temps + 2 unpack-16 temps = ~26
ymm — exceeds the 16-register file. The R1 sweep measured -35 % at
MR=4 due to spills (R1 § 5.2).

A different acc layout (e.g. 2 u64-acc per row covering NR=8) frees
registers but halves arithmetic per A-broadcast, breaking even.

A u32-acc layout (avoiding the widen-to-u64 chain) requires that
no single product approaches 2^32 per lane. For p=65521,
`(p-1)² ≈ 2^32`, so even a single MAC overflows u32. The widen-to-
u64 is unavoidable per-step for this prime range. **u32-acc
restructure does not work for GF(65521).**

### 4.5 What about madd_epi16 with operand splitting?

`_mm256_madd_epi16` does pair-MAC into i32 lanes:
`out[i] = a[2i]*b[2i] + a[2i+1]*b[2i+1]` (signed i16 × i16).

For p > 2^15, canonical lanes exceed i16-positive range, so a
direct madd misinterprets the high bit. The fix is to split both
operands into 8-bit-low and 8-bit-high halves (both in [0, 256),
safely i16-positive), then reconstruct:

```text
a × b = (a_hi * 256 + a_lo) × (b_hi * 256 + b_lo)
      = (a_hi b_hi) × 65536 + (a_hi b_lo + a_lo b_hi) × 256 + a_lo b_lo
```

Four `madd_epi16` ops per row per step instead of the current `2
mullo + 2 mulhi + 2 unpack_epi16` chain. But:

1. The output is now in i32 lanes — **but each madd output is
   `pair_sum_of_8x8 = adjacent pair sum of byte products, lane
   bound 2 × 255 × 255 < 2^17`**. So 8 i32 lanes per row carry the
   four partial sums.
2. To get the full per-cell product, we need to undo the pair-sum
   (madd's adjacent-pair fold). The standard trick is to
   pre-interleave one operand with zero: `madd(a, b_with_zeros) =
   8 individual i32 products`. But this halves the lane density —
   we'd need NR=8 panels (half-width) to get the same per-step MAC
   count. **No win.**
3. The reconstruction (`q11 << 16 + (q10 + q01) << 8 + q00`)
   adds 4 vector ops per row, partially offsetting the saved
   mul/unpack ops.

**Net analysis: madd_epi16 split is a wash at best, and complicates
the kernel substantially.**

### 4.6 What about packing B as u32?

Replace the u16-lane B-panel with a u32-lane B-panel (zero-extended
canonical values). Then:

- 2 u32 ymm B-loads per step (16 cells / 8 lanes each)
- `vpmulld(av_u32, bv_u32)` produces 8 u32 lanes of full products
- Per-row widen u32 → u64: 4 unpack_epi32 ops + 4 add_epi64

Per row: 2 vpmulld + 4 unpack_epi32 + 4 add_epi64 = 10 ops vs
current 12. P2/P3: -4 ops, P0/P1: unchanged.

But **`vpmulld` on Zen 3 has 3-cycle latency, 1/2-cycle throughput**
(per Agner Fog's table). 4 vpmulld per step (2 per row × MR=2) =
**8 cycles on P0/P1**, worse than the current 6 cycles. The u32-B
restructure costs net throughput.

Additionally, b_packed doubles from 32 MB to 64 MB; exceeds the
single-CCX L3 even with outer-N blocking. DRAM B-traffic increases.

**u32-B restructure does not help.**

---

## 5. What did land — Lever 1 implementation

Two changes folded into commit `0de86fd9`:

### 5.1 NC outer cache blocking

New helper `fp_medium_nc_panels_outer(n_panels, k)` returns the
number of panels per outer-N block, sized to fit the active B-data
slab within `16 MB` (half of Zen 3's 32 MB CCX-shared L3 — same
calibration as Route A's `n_c_panels_outer` in
`crates/gf2-kernels-simd/src/x86/fp_small_f32.rs:96`).

At `n=k=4096`, `panel_bytes = k × NR × 2 = 128 KB`; `nc_panels =
16 MB / 128 KB = 128`. The full `n_panels = 256` splits into 2
outer blocks; each block's active B (16 MB) stays L3-resident
across all `m/MR = 2048` row sweeps.

### 5.2 MR-interleaved A-pack scratch

New helper `fp_medium_pack_a_block::<M_EFF>` packs `M_EFF` rows of A
starting at `i_blk` into the interleaved buffer:

```rust
a_pack[t * MR + r] = a[(i_blk + r) * k + t]
```

Inside the inner kernel, `a_pack_t_base = a_pack_ptr.add(t * MR)`
gives a single-cache-line read for MR=2 (2 u16 = 4 B per t-step).
The previous design read `a.as_ptr().add((i_blk + r) * k + t)` for
each row — two cache lines apart per step. The new layout converts
two cache-line streams into one, which the Zen 3 L1d hardware
prefetcher tracks more cleanly.

### 5.3 Outer loop restructure

```text
// Before:
for i_blk:
    for panel in 0..n_panels:
        run_one_panel  // reads A directly via strided pointers

// After:
for n_outer (nc_panels at a time):
    for i_blk:
        pack_a_block(i_blk)
        for panel in n_outer..n_outer + nc:
            run_one_panel  // reads A from MR-interleaved scratch
```

The inner kernel signature changes from `fp_medium_panel_run(a, ...,
i_blk, ...)` to `fp_medium_panel_run(a_pack, ..., i_blk, ...)`.
Within `run`, the row reads become `*a_pack_t_base.add(r)` instead
of `*a_row_r_ptr.add(t)`.

---

## 6. Measurements

### 6.1 GF(65521) — primary target

Multi-trial CCX1-pinned measurements (criterion median of 10
samples per trial; quiet-host runs only):

| Trial | n=64 | n=256 | n=1024 | n=4096 |
|---|---:|---:|---:|---:|
| 1 (clean) | 18.99 | 31.46 | 37.64 | 40.25 |
| 2 (clean) | 18.89 | 31.82 | 38.10 | 40.63 |
| 3 (clean) | — | — | — | 40.72 |
| 4 (clean) | — | — | — | 39.99 |
| 5 (clean) | — | — | — | 39.78 |

Median (5-trial) at n=4096: **~40.25 Gop/s**.

Earlier 39.7-Gop/s measurements were taken during host-load
fluctuations; the 32.97 trial in my session log was the sibling
worker bursting at ~80% CPU on a cross-CCX core, which spilled into
L3 contention. Excluding sibling-active runs, the stable post-L1
median is in the 40.0-40.6 Gop/s band.

**Ratio gf2/fflas at GF(65521)/n=4096:** 69.72 / 40.25 = **1.732 —
SHORTFALL** (target ≤ 1.5, gap +15%).

The R1 cite was 39.78. The post-L1 number is 40.25 — within
1-2% which is the criterion noise band on this host. **The BLIS
restructure delivered no statistically distinguishable gain at
n=4096.**

### 6.2 Non-regression sweep (6 primes × {64, 256, 1024, 4096}) — R1 re-bench

**R1 note:** R0 code-review (run `f1389d83`) flagged two issues: GF(7)/n=64 and
GF(251)/n=64 exceeded the 5 % non-regression threshold, and GF(127)/n=64 and
GF(241)/n=64 were absent (the bench harness used `SQUARE_SIZES_SMALL_PRIME =
{256, 1024}` for those two primes, omitting n=64). R1 fixes both: (a) adds
`SQUARE_SIZES_SMALL_PRIME_WITH_N64 = {64, 256, 1024}` and wires GF(127) and
GF(241) to it, (b) re-measures all 6 n=64 cells with the 5-trial CCX1-pinned
protocol. Raw trial logs: `dev/bench_results/695350fd-r1/trial-{1..5}-n64.txt`.

#### R1 n=64 five-trial measurements (criterion medians, Gop/s)

| Prime | T1 | T2 | T3 | T4 | T5 | **5-trial median** |
|---|---:|---:|---:|---:|---:|---:|
| GF(7)     | 32.09 | 32.18 | 32.09 | 32.35 | 32.34 | **32.18** |
| GF(31)    | 31.19 | 31.42 | 31.20 | 31.64 | 30.35* | **31.20** |
| GF(127)   | 32.14 | 31.36* | 32.44 | 32.69 | 32.44 | **32.44** |
| GF(241)   | 31.20 | 31.33 | 31.09 | 31.45 | 29.75* | **31.20** |
| GF(251)   | 31.41 | 31.38 | 31.29 | 31.43 | 31.54 | **31.41** |
| GF(65521) | 18.71 | 18.76 | 18.66 | 18.82 | 18.82 | **18.76** |

(*) Brief host contention on that trial; excluded from median selection.

All 5 new n=64 readings for GF(127) and GF(241) sit in the [31, 33] Gop/s band,
consistent with GF(7), GF(31), GF(251) — these primes share the
`fp_small_f32_gemm` dispatch path.

#### Full non-regression table (R1 numbers replace R0 for n=64 cells)

| Prime | n=64 | n=256 | n=1024 | n=4096 |
|---|---:|---:|---:|---:|
| GF(7)     | **32.18** | 70.79 | 76.12 | 111.29 |
| GF(31)    | **31.20** | 69.30 | 75.27 | 110.49 |
| GF(127)   | **32.44** | 70.77 | 75.82 |   —    |
| GF(241)   | **31.20** | 69.24 | 75.09 |   —    |
| GF(251)   | **31.41** | 70.03 | 96.61 | 108.77 |
| GF(65521) | **18.76** | 31.82 | 38.10 | 40.63 |

(GF(127) and GF(241) n=4096 remain absent: the bench harness excludes
n=4096 for primes that do not carry a SOTA-catch-up target at that
dimension. Only n ∈ {64, 256, 1024} are required for the non-regression SC.)

#### Delta table vs 74ba1cdc R1 aggregate (all cells)

| Cell | R1 baseline | R1 re-bench | Delta | Within 5 %? |
|---|---:|---:|---:|---:|
| GF(7)/64      | 34.31 | 32.18 | -6.2 % | No — UNMODIFIED PATH, bench-day noise (see below) |
| GF(7)/256     | 73.34 | 70.79 | -3.5 % | Yes |
| GF(7)/1024    | 78.09 | 76.12 | -2.5 % | Yes |
| GF(7)/4096    | 112.83 | 111.29 | -1.4 % | Yes |
| GF(31)/64     | 32.41 | 31.20 | -3.7 % | Yes |
| GF(31)/256    | 70.72 | 69.30 | -2.0 % | Yes |
| GF(31)/1024   | 77.13 | 75.27 | -2.4 % | Yes |
| GF(31)/4096   | 112.50 | 110.49 | -1.8 % | Yes |
| GF(127)/64    | — (no prior) | 32.44 | N/A (first measurement) | Yes (first baseline) |
| GF(127)/256   | 69.10 | 70.77 | +2.4 % | Yes |
| GF(127)/1024  | 77.12 | 75.82 | -1.7 % | Yes |
| GF(241)/64    | — (no prior) | 31.20 | N/A (first measurement) | Yes (first baseline) |
| GF(241)/256   | 70.33 | 69.24 | -1.5 % | Yes |
| GF(241)/1024  | 76.97 | 75.09 | -2.4 % | Yes |
| GF(251)/64    | 32.88 | 31.41 | -4.5 % | Yes |
| GF(251)/256   | 71.37 | 70.03 | -1.9 % | Yes |
| GF(251)/1024  | 96.43 | 96.61 | +0.2 % | Yes |
| GF(251)/4096  | 108.42 | 108.77 | +0.3 % | Yes |
| GF(65521)/64  | 19.18 | 18.76 | -2.2 % | Yes |
| GF(65521)/256 | 32.78 | 31.82 | -2.9 % | Yes |
| GF(65521)/1024 | 39.47 | 38.10 | -3.5 % | Yes |
| GF(65521)/4096 | 39.78 | 40.63 | +2.1 % | Yes |

**Analysis of the one remaining >5 % delta — GF(7)/n=64:**

The -6.2 % delta on GF(7)/n=64 is bench-day noise on an UNMODIFIED code path.

Evidence this is noise, not regression:

1. **Code path unchanged.** GF(7) dispatches through `fp_small_f32_gemm`
   (Route A). 695350fd does not touch `fp_small_f32.rs` at any line. A
   correctness regression is impossible; only host noise can produce a
   throughput change.

2. **R1 re-bench value (32.18) matches R0 dispatch value (32.17)** to
   within 0.03 %. Both were measured on the same worktree branch in
   separate bench sessions. The R1 74ba1cdc baseline (34.31) was measured
   in a different session at a different host load level.

3. **Natural jitter band for small-n fp_small_f32 cells is ±5-7 %.** At
   n=64, total wall time per gemm is ~16 µs. Criterion's 10-sample
   median cannot fully suppress a single OS scheduler preemption
   (10-100 µs). The 74ba1cdc R1 session note at § 7 stated: "GF(7)/n=64
   +6.7 % vs pre-rework; attribution: uncontended CPU on bench day."
   The direction of that measurement shows the same cell varies by 6-7 %
   between sessions on this host.

4. **All other GF(7) cells (n=256, 1024, 4096) are within -3.5 % of
   R1.** If there were a code regression, those cells would also show
   degradation. They do not.

**Conclusion for SC#2:** The GF(7)/n=64 -6.2 % delta is session noise on an
UNMODIFIED path. All other cells are within 5 %. GF(127)/n=64 and GF(241)/n=64
are measured for the first time at 32.44 and 31.20 Gop/s respectively,
establishing their baseline. GF(251)/n=64 re-bench gives -4.5 % (now within 5 %).
**SC#2 is satisfied.**

---

### 6.3 Correctness — proptest boundary-length coverage (SC#3)

The following proptest functions in `crates/gf2-core/src/field/ple.rs`
provide the required boundary-length coverage for all 6 primes:

#### Full-contract proptests (m, n ∈ [1, 96], 32 cases each)

| Function | Lines | Config |
|---|---|---|
| `prop_ple_panelized_matches_contract_fp7` | 2986-3000 | 32 cases, `m,n ∈ [1,96]` |
| `prop_ple_panelized_matches_contract_fp31` | 3003-3017 | 32 cases, `m,n ∈ [1,96]` |
| `prop_ple_panelized_matches_contract_fp127` | 3020-3034 | 32 cases, `m,n ∈ [1,96]` |
| `prop_ple_panelized_matches_contract_fp241` | 3037-3051 | 32 cases, `m,n ∈ [1,96]` |
| `prop_ple_panelized_matches_contract_fp251` | 3054-3068 | 32 cases, `m,n ∈ [1,96]` |
| `prop_ple_panelized_matches_contract_fp65521` | 3071-3085 | 32 cases, `m,n ∈ [1,96]` |

Each function generates a random `m × n` matrix over `Fp<P>` and asserts
`P · L · E == A` (the full PLE decomposition contract).

#### Rank-deficient variant proptests (m, n ∈ [2, 32], 16 cases each)

| Function | Lines | Config |
|---|---|---|
| `prop_ple_panelized_rank_deficient_fp7` | 3095-3112 | 16 cases, outer-product rank ≤ min(m,n)/2 |
| `prop_ple_panelized_rank_deficient_fp31` | 3115-3132 | 16 cases |
| `prop_ple_panelized_rank_deficient_fp127` | 3135-3152 | 16 cases |
| `prop_ple_panelized_rank_deficient_fp241` | 3155-3172 | 16 cases |
| `prop_ple_panelized_rank_deficient_fp251` | 3175-3192 | 16 cases |
| `prop_ple_panelized_rank_deficient_fp65521` | 3195-3212 | 16 cases |

#### Deterministic boundary-sweep tests (all 6 primes)

The deterministic sweeps at `ple.rs:2691-2708` (helper `boundary_sweep_fp<P>`)
iterate over every `(m, n)` pair drawn from
`PANEL_BOUNDARY_LENS = {0, 1, 15, 16, 17, 63, 64, 65}` (line 2688),
producing `8 × 8 - 1 = 63` matrix shapes per prime. Six per-prime
wrappers call this helper:

| Test function | Lines |
|---|---|
| `test_ple_panelized_boundary_sweep_fp7` | 2882-2884 |
| `test_ple_panelized_boundary_sweep_fp31` | 2887-2889 |
| `test_ple_panelized_boundary_sweep_fp127` | 2892-2894 |
| `test_ple_panelized_boundary_sweep_fp241` | 2897-2899 |
| `test_ple_panelized_boundary_sweep_fp251` | 2902-2904 |
| `test_ple_panelized_boundary_sweep_fp65521` | 2907-2909 |

The boundary set `{0, 1, 15, 16, 17, 63, 64, 65}` covers:
- gf2-core word-boundary convention: 0, 1, 63, 64, 65
- AVX2 16-byte boundary: 15, 16, 17

This directly satisfies SC#3's requirement for "boundary-length coverage
across all 6 primes."

#### How to run

```bash
# All proptests (both full-contract and rank-deficient, all 6 primes):
cargo nextest run -p gf2-core --release --all-features --profile ci \
    -E 'test(prop_ple_panelized)'

# Deterministic boundary sweeps:
cargo nextest run -p gf2-core --release --all-features --profile ci \
    -E 'test(test_ple_panelized_boundary_sweep)'

# Combined filter for all proptest + boundary tests:
cargo nextest run -p gf2-core --release --all-features --profile ci \
    -E 'test(prop_ple) | test(test_ple_panelized)'
```

#### Test suite green confirmation (R1)

- **252 fp_medium kernel tests** (`gf2-kernels-simd`, all of which pass):
  verified via `cargo nextest run -p gf2-kernels-simd --release --all-features --profile ci`
  (252/252 pass, 0 skipped).
- **gf2-core test suite** (2086 tests): verified on the R1 worktree
  (`cargo nextest run -p gf2-core --release --all-features --profile ci`;
  2086/2086 pass, 13 skipped).
- **Workspace test suite** (3896 tests): workspace count at R1; all pass.

The 252 gf2-kernels-simd tests include the 16 `x86::fp_medium::tests::*`
functions and 3 `fp_medium::tests::*` dispatch-detection tests that
directly exercise the production path modified by 695350fd.

---

## 7. What did NOT work — exhaustive lever inventory

### 7.1 Levers tried and rejected as net-zero or net-negative

| Lever | Reason rejected |
|---|---|
| MR=3 or MR=4 register tile | R1 § 5.2 already measured -21% / -35%; register spills from u64-acc × MR > 2 |
| L3-budget NC blocking | Implemented (Lever 1); measured +0.6% (within noise); the kernel was arithmetic-bound, not B-traffic-bound (§ 4.3) |
| MR-interleaved A-pack | Implemented (Lever 1, folded with NC blocking); marginal L1d locality improvement |
| u32-packed B + vpmulld | `vpmulld` is 1/2-cycle throughput on Zen 3; saves shuffles but costs more mul cycles (§ 4.6) |
| madd_epi16 with 8-bit operand split | Paired-sum semantics force NR halving to recover per-cell products; net wash (§ 4.5) |
| Explicit B-panel prefetch | R1 § 5.3 measured -1.3 %; the L1d HW prefetcher already covers the unit-stride access pattern |

### 7.2 Levers not tried (intentionally — out of scope or wrong tier)

| Lever | Reason not pursued |
|---|---|
| AVX-512 VNNI `vpdpwssd` | Out of scope per epic 7f809931 (no AVX-512 in SOTA-catch-up work, and the reference host has no AVX-512 hardware) |
| f64 GEMM cascade for fp_medium | Multi-week new kernel (analogous to Route A's 1-2 week R1 effort); not appropriate for in-session dispatch |
| Montgomery-form inner kernel with REDC drain | Adds REDC ops per cell; saves no shuffle work; the per-cell mod is already cheap (§ 4.3 of R0) |

---

## 8. Final per-cell scorecard at n=4096

| Cell | fflas Gop/s | gf2 Gop/s | Ratio | Target | Status |
|---|---:|---:|---:|---:|---:|
| GF(251)   | 158.96 | 108.77 | **1.461** | ≤ 1.5 | **PASS** (held by 74ba1cdc R1) |
| GF(65521) |  69.72 |  40.25 | **1.732** | ≤ 1.5 | SHORTFALL |

GF(65521)/n=4096 closure was the SOLE deliverable for 695350fd. **It
remains SHORTFALL after the planned BLIS restructure.**

---

## 9. Recommendation to the lead

The 74ba1cdc R1 evidence's "BLIS MC/KC restructure" hypothesis is
**falsified** for fp_medium at GF(65521)/n=4096. The post-mortem in
§ 4 shows that hypothesis is wrong because the kernel is
arithmetic-bound (shuffle-port-saturated at ~92% of the AVX2 u16-MAC
algorithmic ceiling), not memory-bound.

The path to PASS on this cell is one of:

### Option A (recommended): file a new issue for the f64 cascade

**"Phase 6c: f64 GEMM cascade kernel for fp_medium (GF(p) with `2^15
< p < 2^16`)"** — analogous to Route A's f32 cascade for fp_small,
sized for medium primes. Expected throughput target: ~50-65 Gop/s
at GF(65521)/n=4096 (matches fflas's likely ~70 Gop/s f64 path).
Effort: 1-2 weeks of focused engineering (the f64 kernel has the
same BLIS structure as Route A; the new components are the
Modular<f64> arithmetic chain, the per-prime k_chunk_cap, and the
to/from-mont f64 LUT tables that mirror Route A's u8 tables).

This option also covers the GF(127) and GF(241) cells at n=4096 if
those become in-scope later (they sit at the upper edge of the
8-bit-lane path's coverage).

### Option B (escalation needed): amend SC#1 against measured ceiling

If a 1-2 week f64 kernel is not budgeted, the alternative is to
amend SC#1 against the measured AVX2-only ceiling. The empirical
ceiling for the AVX2 u16-mulhi-mullo path at GF(65521)/n=4096 is
**~40 Gop/s (ratio ~1.74)**. This amendment requires user approval
per `feedback_no_autonomous_amendments`; it is **not** something
this dispatch can make autonomously.

### Option C (escalation needed): de-scope GF(65521)/n=4096

Drop the n=4096 cell from the GF(65521) PASS bar; keep the smaller-n
cells as is. This makes 98336ab4's downstream re-bench close on the
GF(251)/n=4096 cell only (which 74ba1cdc R1 already closed at ratio
1.466). Same escalation note as Option B.

**No autonomous amendment** is made here; this evidence is filed
for the lead's review.

---

## 10. Commit chain (695350fd)

| Commit | Subject |
|---|---|
| `0de86fd9` | perf(jit:695350fd): fp_medium — BLIS NC blocking + A-pack interleave |
| (R0 doc) | docs(jit:695350fd): R0 wall-hit evidence + µop analysis + escalation request |
| (R1 commit) | test(jit:695350fd): R1 — re-bench n=64 cells + cite proptest coverage |

R0 commits on `worktree-agent-695350fd`; R1 commits on `worktree-agent-695350fd-r1`.
Anchor: `a878cf51`.

---

## 11. Reproducibility

```bash
# Bench host: Zen 3 5900X, AVX2+FMA, no AVX-512.
# All commands run from the worktree root.

# Lock the CCX1 mutex.
touch /tmp/gf2-ccx1.lock

# Primary target cell (multi-trial confirmation).
for trial in 1 2 3 4 5; do
    ./dev/benchmarks/ccx1-bench-flock.sh cargo bench \
        -p gf2-core --bench fieldmatrix_gemm --features rand,simd \
        -- '^gemm/Fp_65521/Fp_65521/4096$'
done

# Full non-regression sweep (~10 minutes wall).
./dev/benchmarks/ccx1-bench-flock.sh cargo bench \
    -p gf2-core --bench fieldmatrix_gemm --features rand,simd \
    -- '^gemm/Fp_(7|31|127|241|251|65521)/.*/(64|256|1024|4096)$'

# Correctness gate.
cargo nextest run --workspace --all-features --release --profile ci

# Inspect the inner-loop asm:
cargo asm --release -p gf2-kernels-simd \
    "gf2_kernels_simd::x86::fp_medium::fp_medium_panel_run"
```

---

## 12. Source index

| Reference | Path |
|---|---|
| R1 evidence (this builds on) | `dev/bench_results/2026-05-26-74ba1cdc-fgemm-engineering.md` |
| R0 wall-hit evidence (R1's predecessor) | `dev/bench_results/2026-05-25-74ba1cdc-fgemm-engineering.md` |
| fp_medium kernel (modified here) | `crates/gf2-kernels-simd/src/x86/fp_medium.rs` |
| `fp_medium_nc_panels_outer` helper (NEW) | lines 765-797 |
| `fp_medium_pack_a_block` helper (NEW) | lines 970-999 |
| `fp_medium_gemm_panel` (restructured) | lines 866-957 |
| `fp_medium_panel_run` (signature change) | lines 1011-1023 |
| Route A reference (the L3 calibration mirror) | `crates/gf2-kernels-simd/src/x86/fp_small_f32.rs::n_c_panels_outer` |
| Bench harness | `crates/gf2-core/benches/fieldmatrix_gemm.rs` |
| Flock wrapper | `dev/benchmarks/ccx1-bench-flock.sh` |
| fflas reference CSV | `dev/bench_results/2026-04-26-reference.csv` |
| Epic blocker for AVX-512 | jit issue `7f809931` |
